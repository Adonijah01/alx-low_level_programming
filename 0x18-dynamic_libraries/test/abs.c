#include "main.h"
/**
 *_abs - cmpute the absolute value of an intgr
 *@n: print charactr
 *Return: 0
 */
int _abs(int n)
{
if (n > 0)
{
return (n);
}
else
return (-n);
}

/**
 * Adonijah Kiplimo
 */
